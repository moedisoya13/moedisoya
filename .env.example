# Database
DATABASE_URL="file:./dev.db"

# Web Push (VAPID Keys - 배포 시 새로 생성 필요)
# npx web-push generate-vapid-keys 로 생성
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_EMAIL="mailto:your-email@example.com"
